# ===================================
# Server Configuration
# ===================================
PORT=5002

# SSL Configuration
# Note: Both formats are supported for compatibility
SSL_KEY_FILE=ssl/localhost-key.pem
SSL_CRT_FILE=ssl/localhost-cert.pem
SSL_KEY_PATH=ssl/localhost-key.pem  # Legacy support
SSL_CERT_PATH=ssl/localhost-cert.pem  # Legacy support

# ===================================
# OAuth2 Configuration
# ===================================
# Gmail OAuth2
GMAIL_CLIENT_ID=your-google-client-id
GMAIL_CLIENT_SECRET=your-google-client-secret
GMAIL_REDIRECT_URI=https://localhost:5002/auth/gmail/callback

# Outlook OAuth2
OUTLOOK_CLIENT_ID=your-outlook-client-id
OUTLOOK_CLIENT_SECRET=your-outlook-client-secret
OUTLOOK_TENANT_ID=your-outlook-tenant-id
OUTLOOK_REDIRECT_URI=https://localhost:5002/auth/outlook/callback

# ===================================
# Frontend Configuration
# ===================================
REACT_APP_BACKEND_URL=https://localhost:5002
FRONTEND_REDIRECT_URL=http://localhost:5003/
FRONTEND_POSTAUTH_ROUTE=/home

# Frontend Customization
REACT_APP_USE_GROUP_LOGO=false
REACT_APP_TITLE=ImfrisivMail – Organize your emails
REACT_APP_DESCRIPTION="A brief description of the app"
REACT_APP_KEYWORDS=ImfrisivMail,email,productivity

# ===================================
# Backend Services
# ===================================
# CORS: Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://localhost:5003,http://localhost:3000

# Classifier service URL (POST /classify)
CLASSIFIER_URL=http://localhost:5001

# Backend network configuration
BACKEND_HOST=localhost
BACKEND_PORT=5002

# ===================================
# Email Classification
# ===================================
# Comma-separated list of classification categories
CLASSIFICATION_CATEGORIES=important,spam,newsletter,social,promotional,personal,business,automated

# ===================================
# API Keys (Never commit real keys to version control!)
# ===================================
# Required for AI-powered features
OPENAI_API_KEY=your-openai-api-key
# Optional: For advanced features
PERPLEXITY_API_KEY=your-perplexity-key
# Optional: Google Gemini (Generative Language) API key
# Example: GOOGLE_GEMINI_API_KEY=your-google-gemini-api-key
GOOGLE_GEMINI_API_KEY=your-google-gemini-api-key
# Compose / LLM integration (server-side)
# Set `OPENAI_API_KEY` (preferred) or `PERPLEXITY_API_KEY` on the server to enable AI compose features.
# Do NOT expose these keys in frontend `REACT_APP_` variables as they would be public.

# ===================================
# Notes & Documentation
# ===================================
# Required Variables:
# - GMAIL_CLIENT_ID & GMAIL_CLIENT_SECRET: For Gmail OAuth2 authentication
# - OUTLOOK_CLIENT_ID & OUTLOOK_CLIENT_SECRET: For Outlook OAuth2 authentication
# - SSL certificate files must be placed in the specified paths
#
# Optional Variables:
# - FRONTEND_REDIRECT_URL: Fallback for OAuth2 redirects if not provided by frontend
# - FRONTEND_POSTAUTH_ROUTE: Route to redirect after authentication (default: /home)
# - CLASSIFICATION_CATEGORIES: Customize email classification categories
# - ALLOWED_ORIGINS: Configure CORS for additional frontend URLs
#
# Security Note:
# - Never commit real API keys or secrets to version control
# - Use environment variables or a secure secret management system in production
